[package]
name = "foundry-macros"
version = "2.0.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
# rkyv ELIMINADO de aquí. Mitiga tiempos de compilación inflados de la macro.
syn = { version = "2.0", features = ["full", "extra-traits"] }
quote = "1.0"
proc-macro2 = "1.0"
