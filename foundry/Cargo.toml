[package]
name = "foundry"
version = "1.0.0"
edition = "2021"
description = "Fachada pública limpia para el ecosistema foundry"

[dependencies]
foundry-core = { path = "../foundry-core" }
foundry-macros = { path = "../foundry-macros" }
bincode = "1.3"
serde = { version = "1.0", features = ["derive"] }
